{
  "name": "@poker-club/mobile",
  "version": "0.1.0",
  "main": "expo-router/entry",
  "private": true,
  "//qrcode": "Pinned EXACTLY, not caret-ranged: src/lib/qr.ts imports qrcode/lib/core/qrcode.js, which is internal to the package and not covered by its semver promise. The renderers behind the main entry need canvas or fs and cannot run here.",
  "//expo-updates": "Nothing imports it. It is here because `eas update` refuses to publish without it, and publishing is how the app reaches an iPhone with no paid Apple Developer account behind it — see docs/live-test.md. In Expo Go the native side is Expo Go's own, so the package is inert at runtime. ~29.0.19 is SDK 54's version from bundledNativeModules.json, which is exactly what `expo install` would have chosen.",
  "//sdk": "Expo SDK 54, pinned to match the Expo Go build in the App Store. Every version below comes from SDK 54's own bundledNativeModules manifest — do not bump one on its own.",
  "dependencies": {
    "@expo/metro-runtime": "~6.1.2",
    "@poker-club/core": "*",
    "@react-native-async-storage/async-storage": "2.2.0",
    "@supabase/supabase-js": "^2.112.3",
    "expo": "~54.0.36",
    "expo-brightness": "~14.0.8",
    "expo-clipboard": "~8.0.8",
    "expo-constants": "~18.0.13",
    "expo-crypto": "~15.0.9",
    "expo-keep-awake": "~15.0.8",
    "expo-linking": "~8.0.12",
    "expo-router": "~6.0.24",
    "expo-sqlite": "~16.0.10",
    "expo-status-bar": "~3.0.9",
    "expo-updates": "~29.0.19",
    "qrcode": "1.5.4",
    "react": "19.1.0",
    "react-dom": "^19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0",
    "react-native-svg": "15.12.1",
    "react-native-url-polyfill": "^3.0.0",
    "react-native-web": "^0.21.2"
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "typescript": "~5.9.2"
  },
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "//publish:go": "Put the app where a phone can open it in Expo Go with NO dev server and NO laptop — the update is fetched from Expo over any connection. EAS_PROJECT=go is the load-bearing part: it leaves runtimeVersion off, and an update carrying one is refused by Expo Go. Needs `npx eas-cli login` as the account that owns the project, and the phone's Expo Go signed into the same account. docs/live-test.md is the whole procedure.",
    "publish:go": "EAS_PROJECT=go npx eas-cli@latest update --branch expo-go --message \"$(git log -1 --pretty=%s)\"",
    "//export:web": "A static build of the real app, for scripts/ui-check.mjs to drive in a headless browser. Same components, same tokens, at the frames' 402x874.",
    "export:web": "expo export --platform web --output-dir .web",
    "typecheck": "tsc --noEmit"
  }
}
