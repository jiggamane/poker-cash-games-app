{
  "expo": {
    "name": "The Poker Club",
    "slug": "poker-club",
    "version": "0.1.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "//": "Two themes, dark and light \u2014 the OS decides which. Never pin this to one.",
    "userInterfaceStyle": "automatic",
    "//scheme": "Watchers open a share link that deep-links straight into the session.",
    "scheme": "pokerclub",
    "ios": {
      "supportsTablet": false,
      "bundleIdentifier": "com.pokerclub.app"
    },
    "android": {
      "package": "com.pokerclub.app",
      "adaptiveIcon": {
        "backgroundColor": "#0A0A0B",
        "foregroundImage": "./assets/android-icon-foreground.png",
        "backgroundImage": "./assets/android-icon-background.png",
        "monochromeImage": "./assets/android-icon-monochrome.png"
      },
      "predictiveBackGestureEnabled": false
    },
    "web": {
      "favicon": "./assets/favicon.png"
    },
    "//updates": "NOT IN THIS FILE, ON PURPOSE \u2014 and no longer missing either: app.config.js adds updates.url and runtimeVersion when EAS_PROJECT says which kind of build is being made. Expo Go supplies its own runtime and REFUSES any update carrying a runtimeVersion, so the two cases genuinely differ and cannot both live in a static file. Read the comment in app.config.js before changing this.",
    "//eas": "THE EAS PROJECT ID IS NOT IN THIS FILE, ON PURPOSE. An id here makes Expo Go prompt for an Expo login on every start, which breaks the scan-a-QR-code loop for no benefit while developing. app.config.js adds it \u2014 with updates.url, and with runtimeVersion when a standalone build asks for it \u2014 only when EAS_PROJECT is set, so publishing and developing each get the config they need. The project is 938b4629-9a41-4ddf-bcd8-86bb4e4696b3, and app.config.js is the one place it is written down."
  }
}
